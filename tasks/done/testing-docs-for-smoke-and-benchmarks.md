# Testing docs for smoke and benchmarks

## Goal

Document the focused UI smoke and latency benchmark commands that now exist.

## Scope

- Update `docs/testing.md`.
- Mention `npm run smoke:ui` as the main Electron UI regression command.
- List the focused UI smoke commands for debugging.
- List focused latency benchmark commands.

## Gates

- `git diff --check`
